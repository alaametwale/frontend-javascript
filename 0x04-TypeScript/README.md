# 0x04. TypeScript

## 📘 Description
This project introduces the basics of **TypeScript** in frontend development.  
It includes tasks focused on interfaces, classes, and type checking.

## 🧠 Learning Objectives
- Understand TypeScript syntax and types  
- Learn how to configure `tsconfig.json`  
- Compile TypeScript to JavaScript using `tsc`  
- Apply interfaces and classes in real-world examples  

## 📂 Structure
- `task_0/` → Creating an interface for a student  
- `task_1/` → Working with classes and inheritance  
- Other directories contain additional exercises and configurations  

## ⚙️ Setup
To install dependencies and compile TypeScript:
```bash
npm install
npx tsc
